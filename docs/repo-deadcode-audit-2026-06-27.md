# Repo dead-file audit — 2026-06-27

A review list of files that *look* unused, for you to eyeball and delete. Generated by a
reachability sweep (imports/invocations) across RN, backend, tools, docs, qa, assets.

**Important:** this is a candidate list, not a safe auto-delete. "Tree-shaking" of the *bundle*
already happens at build time; this is about deleting orphaned *source files* from the repo.
Manual/runbook scripts and dynamically-referenced files can look dead while being used — so the
tiers below reflect how much I actually verified.

---

## Tier A — Verified safe-ish (low risk)

- [ ] **`qa/raw-normalization-regressions/`** (~9.1 MB, 84 files) — was consumed only by the now-deleted
  Swift regression scripts; **no surviving references** (verified by grep).
- [ ] **Expo/CRA boilerplate assets** (~37 KB) in `apps/spotlight-rn/assets/`:
  `react-logo.png`, `react-logo@2x.png`, `react-logo@3x.png`, `expo-badge.png`,
  `expo-badge-white.png`, `expo-logo.png` — template leftovers, no references.
- [ ] **Ruled-out visual experiments** (match the earlier "multiview / blended / letterbox all failed"
  decision; no automation/doc references):
  `tools/build_blended_visual_index.py`, `tools/build_multiview_visual_index.py`,
  `tools/eval_multiview_leave_one_out.py`.

## Tier B — Likely dead, glance before deleting

7 RN source files with **no import sites found** (several line up with features we removed):
- [ ] `src/features/portfolio/components/recent-sales-section.tsx` — sales UI (sell feature was removed).
- [ ] `src/features/pricing/use-suggested-discount.ts` — vendor discount hook (sell feature).
- [ ] `src/features/cards/components/card-hero.tsx` — superseded hero component.
- [ ] `src/features/auth/components/wave-background.tsx` — SVG hero; the live auth hero is the WebView one.
- [ ] `src/lib/use-image-dominant-color.ts` — dominant-color hook (react-native-image-colors).
- [ ] `src/features/catalog/catalog-search-helpers.ts` — catalog grouping helpers.
- [ ] `src/features/scanner/screens/scanner-search-launcher.tsx` — dark SearchField wrapper.

Other orphaned assets (`apps/spotlight-rn/assets/`):
- [ ] `auth-hero.png` (~540 KB) — superseded by `auth-wave-hero.png` (which IS used).
- [ ] `logo-glow.png` (~324 KB), `tutorial-web.png` (~58 KB) — no references found.

> Risk note: confirm none are pulled in via a string path / `require.context` / app config before deleting.

## Tier C — Docs to archive (your judgment; docs are cheap to keep)

~23 dated specs whose work has shipped, been superseded, or is deferred — candidates to move to a
`docs/archive/` folder rather than hard-delete. Representative examples:
- Shipped: `auth-flow-redesign-2026-06-15.md` (marked implemented).
- Deferred-indefinite: `ios26-liquid-glass-bars-2026-06-04.md`, `scanner-toggle-removal-plan-2026-05-18.md`,
  `db-size-reduction-plan-2026-06-10.md`, `persistent-scan-upload-queue-followup-2026-06-16.md`.
- Superseded April planning: `looty-ui-design-system-plan-2026-04-16.md`,
  `manual-card-search-v1-spec-2026-04-20.md`, `collection-import-spec-2026-04-20.md`,
  `backend-latency-refactor-spec-2026-04-10.md`, `fallback-ocr-cost-control-checklist-2026-04-17.md`.
- Duplicate: `scanner-lock-on-ux-plan-2026-05-21.md` vs `scanner-live-lock-on-ux-spec-2026-05-21.md` (keep one).

(The 23 docs referenced by `AGENTS.md` / `docs/agent-context-index.md` are source-of-truth — keep.)

---

## DO NOT bulk-delete — false positives the scan flagged

An automated "not referenced by CI" pass flagged ~54 tool scripts as orphaned. **Most are manual
runbook/ops scripts you actively use** — being run by hand (not by automation) is not "dead". Keep:

- **Labeling + retrain pipeline:** `scan_labeling_server.py` (your friend-labeling flow),
  `run_labeling_retrain_cycle.py`, `process_raw_visual_batch.py`, `import_confirmed_scans_to_training.py`,
  `finalize_and_train_show.py`, `build_raw_visual_training_manifest.py`, `train_raw_visual_adapter.py`,
  `build_raw_visual_index.py`, `build_user_photo_rerank_pool.py`, `rerank_pool_*`, `refresh_rerank_pool.sh`.
- **SigLIP2 (LIVE backbone):** `export_siglip_onnx.py`, `tools/backbone_bakeoff/*`.
- **Language probe (LIVE):** `train_jp_en_language_probe.py`.
- **Current EN/JP linker work (in progress):** `build_card_language_links.py`, `import_card_language_links.py`,
  `eval_card_language_links.py`.
- **`qa/raw-footer-layout-check/`** (~137 MB) — the scan called it orphaned, but it's **still used** by the
  live rerank-pool builder (`build_user_photo_rerank_pool.py`) + `export_clip_onnx.py` +
  `local_rerank_full_comparison.py`. **Keep.**

> If you want, the right long-term fix is to install `knip` in `apps/spotlight-rn` for deterministic
> unused-file/-export detection on the TS side, and to tag manual runbook scripts in one index so a
> future audit can tell "manual but live" from "dead".
