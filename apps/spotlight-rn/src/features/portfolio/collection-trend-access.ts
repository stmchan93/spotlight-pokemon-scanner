import type { WatchlistTrendAccess } from '@/features/wishlist/watchlist-trend-access';

/**
 * How much of the "since added" trend Collection rows and tiles show — the
 * same paywall switch as the Watchlist's (`full` | `percentOnly` | `hidden`).
 */
export const COLLECTION_TREND_ACCESS: WatchlistTrendAccess = 'full';

export const SINCE_ADDED_SUFFIX = 'since added';
