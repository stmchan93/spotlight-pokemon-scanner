import { DemoContent } from '@/components/glass-tabs-demo-content';

/** THROWAWAY SPIKE — delete with the rest of `glass-tabs-demo/`. */
export default function GlassTabsDemoCollection() {
  return <DemoContent title="Collection" />;
}
